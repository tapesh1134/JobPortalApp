version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=0000
      - POSTGRES_DB=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - jobportal-network

  redis:
    image: redis:alpine
    container_name: redis
    restart: always
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - jobportal-network

  rabbitmq:
    image: rabbitmq:3-management
    container_name: rabbitmq
    restart: always
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=${RABBITMQ_USER:-guest}
      - RABBITMQ_DEFAULT_PASS=${RABBITMQ_PASS:-guest}
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 15s
      timeout: 10s
      retries: 5
    networks:
      - jobportal-network

  eureka-server:
    image: tapesh/eureka-server:latest
    container_name: eureka-server
    restart: always
    ports:
      - "8761:8761"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8761/actuator/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - jobportal-network

  auth-service:
    image: tapesh/auth-service:latest
    container_name: auth-service
    ports:
      - "8081:8081"
    restart: always
    depends_on:
      postgres: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
      redis: { condition: service_healthy }
      eureka-server: { condition: service_healthy }
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/job_portal_auth
      - SPRING_REDIS_HOST=redis
      - SPRING_RABBITMQ_HOST=rabbitmq
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - JAVA_TOOL_OPTIONS=-Xmx256m
    networks:
      - jobportal-network

  profile-service:
    image: tapesh/profile-service:latest
    container_name: profile-service
    ports:
      - "8082:8082"
    restart: always
    depends_on:
      postgres: { condition: service_healthy }
      eureka-server: { condition: service_healthy }
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/job_portal_profile
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - JAVA_TOOL_OPTIONS=-Xmx256m
    networks:
      - jobportal-network

  job-service:
    image: tapesh/job-service:latest
    container_name: job-service
    ports:
      - "8083:8083"
    restart: always
    depends_on:
      postgres: { condition: service_healthy }
      eureka-server: { condition: service_healthy }
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/job_portal_jobpost
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - JAVA_TOOL_OPTIONS=-Xmx256m
    networks:
      - jobportal-network

  application-service:
    image: tapesh/application-service:latest
    container_name: application-service
    ports:
      - "8084:8084"
    restart: always
    depends_on:
      postgres: { condition: service_healthy }
      eureka-server: { condition: service_healthy }
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/job_portal_application
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - JAVA_TOOL_OPTIONS=-Xmx256m
    networks:
      - jobportal-network

  interview-service:
    image: tapesh/interview-service:latest
    container_name: interview-service
    ports:
      - "8085:8085"
    restart: always
    depends_on:
      postgres: { condition: service_healthy }
      eureka-server: { condition: service_healthy }
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/job_portal_interview
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - JAVA_TOOL_OPTIONS=-Xmx256m
    networks:
      - jobportal-network

  subscription-service:
    image: tapesh/subscription-service:latest
    container_name: subscription-service
    ports:
      - "8086:8086"
    restart: always
    depends_on:
      postgres: { condition: service_healthy }
      eureka-server: { condition: service_healthy }
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/job_portal_subscription
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - JAVA_TOOL_OPTIONS=-Xmx256m
    networks:
      - jobportal-network

  notification-service:
    image: tapesh/notification-service:latest
    container_name: notification-service
    ports:
      - "8087:8087"
    restart: always
    depends_on:
      postgres: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
      redis: { condition: service_healthy }
      eureka-server: { condition: service_healthy }
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/job_portal_notification
      - SPRING_RABBITMQ_HOST=rabbitmq
      - SPRING_REDIS_HOST=redis
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - JAVA_TOOL_OPTIONS=-Xmx256m
    networks:
      - jobportal-network

  analytics-service:
    image: tapesh/analytics-service:latest
    container_name: analytics-service
    ports:
      - "8088:8088"
    restart: always
    depends_on:
      postgres: { condition: service_healthy }
      eureka-server: { condition: service_healthy }
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/job_portal_analytics
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - JAVA_TOOL_OPTIONS=-Xmx256m
    networks:
      - jobportal-network

  jobportal-web:
    image: tapesh/jobportal-web:latest
    container_name: jobportal-web
    ports:
      - "8089:8089"
    restart: always
    depends_on:
      eureka-server: { condition: service_healthy }
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
    networks:
      - jobportal-network

  api-gateway:
    image: tapesh/api-gateway:latest
    container_name: api-gateway
    ports:
      - "8080:8080"
    restart: always
    depends_on:
      eureka-server: { condition: service_healthy }
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE=http://eureka-server:8761/eureka/
      - JAVA_TOOL_OPTIONS=-Xmx256m
    networks:
      - jobportal-network

networks:
  jobportal-network:
    name: jobportal-network

volumes:
  postgres_data: